import { OnInit } from '@angular/core';
export declare class VerticalTimelineCardComponent implements OnInit {
    dateValue: Date;
    timeString: string;
    color: string;
    disabled: boolean;
    isATimelineItem: boolean;
    textColor: string;
    ngOnInit(): void;
    private isLight(hexColor);
    private calculateBrightness(R, G, B);
}
